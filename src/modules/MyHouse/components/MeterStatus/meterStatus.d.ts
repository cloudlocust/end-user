/**
 * Interface for MeterStatus component props.
 */
export interface MeterStatusProps {
    /**
     * House Id of the actual house.
     */
    houseId: number
    /**
     * MeterGuid of the house's meter.
     */
    meterGuid: string
}
